node_modules/
dist/
.next/
build/
.turbo/
.env
.env.local
secrets/*
!secrets/*.example
.env.*.local
.env.production
.aws-forge-output.env
compose.prod.env
!compose.prod.env.example
!**/.env.example
!**/.env.production.example

# TLS (generate on server; never commit keys)
infra/nginx/ssl/

# Accidental tsc output in shared-types src (build goes to dist/)
packages/shared-types/src/**/*.js
packages/shared-types/src/**/*.js.map
packages/shared-types/src/**/*.d.ts
packages/shared-types/src/**/*.d.ts.map
*.log
.DS_Store
.idea/
.vscode/
coverage/
*.tsbuildinfo
*.zip

# Local CodeGraph index/daemon cache (not source, machine-local)
.codegraph/

# Flutter
apps/mobile/.dart_tool/
apps/mobile/.flutter-plugins
apps/mobile/.flutter-plugins-dependencies
apps/mobile/build/
apps/mobile/.packages
# pubspec.lock is intentionally tracked (LOW-12) — apps (not packages) should
# commit their lockfile for reproducible builds; do not re-add this ignore.
apps/mobile/android/.gradle/
apps/mobile/ios/Pods/
apps/mobile/ios/.symlinks/

# Docker volumes
postgres_data/
redis_data/

# Playwright
**/test-results/
**/playwright-report/
**/blob-report/

# Local audit / design-prompt scratch (not product source)
prompt_docs/
new_prompt.md
FORGE*Enterprise_Production_Readiness*
scripts/export-stitch-project.py
scripts/stitch-generate-batch1.py
scripts/stitch-generate-batch1.sh

# Local Cursor rules (optional — commit .cursor/rules/ to share team standards)
.cursor/*
!.cursor/rules/
