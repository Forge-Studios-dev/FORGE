# Phase 20 — QA & Automation

**Status:** Documented with known gap

## Present

- Jest unit/specs across API modules
- Slim HTTP harness pattern
- Flutter unit tests for multipart upload

## Gap

- Workspace may lack hoisted `@nestjs/testing` until `npm install`
- Prefer `npx jest --testPathPatterns=… --watchman=false` locally
