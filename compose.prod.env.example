# Reference-only: paired with docker-compose.prod.reference.yml (self-host topology, NOT FORGE production).
# Copy to `.env` in this directory so Docker Compose substitutes variables in the reference compose file.
# (Compose loads `.env` automatically; do not commit real secrets.) See docs/DEPLOY.md for actual production.

DB_NAME=forge_db
DB_USERNAME=forge
DB_PASSWORD=change_me_strong_password
REDIS_PASSWORD=change_me_redis_password
