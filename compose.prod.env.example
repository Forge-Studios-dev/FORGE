# Copy to `.env` in this directory so Docker Compose substitutes variables in docker-compose.prod.yml
# (Compose loads `.env` automatically; do not commit real secrets.)

DB_NAME=forge_db
DB_USERNAME=forge
DB_PASSWORD=change_me_strong_password
REDIS_PASSWORD=change_me_redis_password
