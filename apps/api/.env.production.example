# Production API — for real Fly.io deployment (secrets via `fly secrets set`), or
# as apps/api/.env.production with the self-host reference topology
# (docker-compose.prod.reference.yml; not FORGE production — see docs/DEPLOY.md).
NODE_ENV=production
PORT=3001

# Neon (recommended for Fly.io): pooled URL from https://console.neon.tech
# Hostname MUST include -pooler (validated at API startup in production)
DATABASE_URL=postgresql://USER:PASSWORD@ep-xxxx-pooler.region.aws.neon.tech/neondb?sslmode=require
DB_POOL_MAX=3
DB_POOL_IDLE_TIMEOUT_MS=120000
# DATABASE_ALLOW_DIRECT_NEON=true  # CLI/migrations only — never for API/worker

# Worker-only MVP cost flags (set on forge-studios-worker via fly secrets, not API):
# DISABLE_EMAIL_DIGEST=true
# DISABLE_STREAM_CLIP_EXPORT=true
# DISABLE_SHORTS_WATCH_PERCENT=true

# Or Docker Compose Postgres:
# DATABASE_URL=postgresql://forge:change_me_strong_password@postgres:5432/forge_db

# Must match Redis service: use password in URL when Redis uses requirepass
REDIS_URL=redis://:change_me_redis_password@redis:6379
REDIS_PASSWORD=change_me_redis_password

JWT_SECRET=replace-with-openssl-rand-base64-64
JWT_EXPIRES_IN=15m
JWT_REFRESH_SECRET=replace-with-different-openssl-rand-value
JWT_REFRESH_EXPIRES_IN=7d

AWS_REGION=ap-south-1
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
S3_BUCKET_NAME=forge-media-prod
CLOUDFRONT_DOMAIN=https://your-cdn.example.com

MUX_TOKEN_ID=
MUX_TOKEN_SECRET=
MUX_WEBHOOK_SECRET=
VIDEO_TRANSCODE_PROVIDER=mux
MUX_INGEST_URL_TTL_SEC=43200

RATE_LIMIT_TTL=60
RATE_LIMIT_MAX=100

WEB_URL=https://forge.app
ADMIN_URL=https://admin.forge.app
