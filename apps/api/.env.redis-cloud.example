# Redis Cloud — paste from https://redis.io/cloud → your database → Connect
# Use the Redis CLI / connection string exactly as shown (redis:// or rediss://).

REDIS_URL=redis://default:YOUR_PASSWORD@YOUR-HOST.db.redis.io:13195

# Test: npm run redis:test
